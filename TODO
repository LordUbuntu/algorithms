- Is there an algorithm to reverse image transformations like box blur? What image transformations are reversable, which ones aren't?
- Add a decrypt for rot13, and a C implementation (which might ironically be easier than the python one?)
